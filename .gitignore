# Build outputs
bin/
obj/
publish/
dist/
*.obj
*.res
*.exp
*.lib
*.dll
*.exe
*.pdb
*.user
*.suo
*.nupkg

# Cloned reference repos (large upstream sources, keep out of git)
reference/

# Local test server runtime files
research/ftpserver/test_host_key
research/ftpserver/__pycache__/
research/ftpserver/root/www/subdir/

# Debug log (absolute paths are intentionally not represented here; .gitignore
# patterns are repository-relative).
remotefs-debug.log

# SSH test keys — NEVER commit
research/keys/
# WinSCP capability-test outputs (session logs)
research/winscp-test/out/

# 开发文档只留本地：仓库对外只发布用户文档（2026-09-17 决定）
docs/

# 安装器自测产物（日志留本地看，不进仓库）
src-setup/*.log*
