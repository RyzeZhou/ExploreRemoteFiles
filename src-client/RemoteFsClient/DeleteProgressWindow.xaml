<Window x:Class="RemoteFsClient.DeleteProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="460" Height="174" ResizeMode="NoResize" WindowStyle="ToolWindow"
        ShowInTaskbar="False" Topmost="True" WindowStartupLocation="Manual">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" FontWeight="SemiBold" FontSize="15"/>
        <TextBlock x:Name="PathText" Grid.Row="1" Margin="0,6,0,10" TextTrimming="CharacterEllipsis" Foreground="#555"/>
        <ProgressBar x:Name="Progress" Grid.Row="2" Height="16" Minimum="0" Maximum="100" IsIndeterminate="True"/>
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            <Button x:Name="CancelButton" Grid.Column="1" Click="OnCancel" Padding="14,3" Margin="12,0,0,0"/>
        </Grid>
    </Grid>
</Window>
