<Window x:Class="RemoteFsClient.SiteEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑站点" Height="380" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
            <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,4"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,4"/>
                <Setter Property="Padding" Value="2"/>
            </Style>
        </Grid.Resources>

        <TextBlock Text="名称"/>
        <TextBox Grid.Column="1" x:Name="TbName"/>
        <TextBlock Grid.Row="1" Text="协议"/>
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="CbType" Margin="0,4">
            <ComboBoxItem Content="sftp"/>
            <ComboBoxItem Content="ftp"/>
            <ComboBoxItem Content="ftps"/>
        </ComboBox>
        <TextBlock Grid.Row="2" Text="主机"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="TbHost"/>
        <TextBlock Grid.Row="3" Text="端口（可空）"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TbPort"/>
        <TextBlock Grid.Row="4" Text="用户名"/>
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="TbUser"/>
        <TextBlock Grid.Row="5" Text="起始路径"/>
        <TextBox Grid.Row="5" Grid.Column="1" x:Name="TbPath"/>

        <TextBlock Grid.Row="6" Text="密码"/>
        <StackPanel Grid.Row="6" Grid.Column="1">
            <PasswordBox x:Name="PbPassword" Margin="0,4" Padding="2"/>
            <TextBlock x:Name="PwdHint" FontSize="11" Foreground="#666" TextWrapping="Wrap"
                       Text="密码将存入 Windows 凭据管理器（DPAPI），不会写入 JSON。留空表示不修改。"/>
        </StackPanel>

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="保存" Click="OnSave" Padding="16,4" Margin="0,0,8,0"/>
            <Button Content="取消" IsCancel="True" Padding="16,4"/>
        </StackPanel>
    </Grid>
</Window>
